{
  "name": "wont.stream",
  "module": "index.html",
  "scripts": {
    "dev": "bun build ./src/index.js --outdir ./dist --target browser --splitting --watch",
    "build": "bun build ./src/index.js --outdir ./dist --target browser --splitting --sourcemap=external --minify"
  },
  "dependencies": {
    "@material-symbols/font-400": "^0.17.3",
    "@material/web": "^1.4.1"
  }
}